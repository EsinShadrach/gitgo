name: Run Compile

on:
  push:
    branches:
      - main
jobs:
  my-job:
    name: Compiling gitgo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1
      - run: bun installl --production
      - run: bun run compile
