{
  "name": "gitgo",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun build ./src/index.ts --minify --compile --outfile ~/dev/cutom-cli/gitgo"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "terminal-link": "^3.0.0"
  }
}
